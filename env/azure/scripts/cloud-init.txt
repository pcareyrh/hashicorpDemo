#cloud-config
packages:
  - epel-release
  - wget
bootcmd:
    - sudo echo "Cloud Init running" > /tmp/test.txt
    - sudo yum install -y ansible
    - sudo wget -O /var/www/apache_install.yml https://raw.githubusercontent.com/pcareyrh/hashicorpDemo/main/env/aws/apache_install.yml
    - sudo ansible-playbook /var/www/apache_install.yml

#cloud-config

# Install additional packages on first boot
#
# Default: none
#
# if packages are specified, this apt_update will be set to true
#
# packages may be supplied as a single package name or as a list
# with the format [<package>, <version>] wherein the specifc
# package version will be installed.
#packages:
#  - epel-release
#  - wget
##  
#runcmd:
#  - mkdir /run/mydir
#  - [yum, install, -y, ansible ]
#  - [ wget, "https://raw.githubusercontent.com/pcareyrh/hashicorpDemo/main/env/aws/apache_install.yml", -O, /run/mydir/apache_install.yml ]
#  - sudo ansible-playbook /run/mydir/apache_install.yml

